list 允许重复 有序 有下标 []
tuple 允许重复 里面的元素不能增加删除修改 只能查看 ()
dict 键值对存在 键 唯一 值：允许重复 {}
set 不允许重复  无序的{}

dict如果需要强行转换 默认会把key当成你要强转类型的元素

list转成dict,只有下面这种情况才能强转
list = [('a',1),('b',2),('c',3)]
list = [['a',1],['b',2],['c',3] ]

全局变量和局部变量
1.什么是全局变量？
2.全局变量的位置问题？在函数之前可以，在函数之后也可以，因为函数是首先加载到内存中，只要不调用，不会有影响
查看全局变量 print(globals()) 返回的是一个字典 __builtins__ <module>  包括了默认加载的和脚本中新建的一些全局变量
       分成 1.系统  2.自定义  局部变量不会在globals中加载
       print(globals())
局部:
1.什么是局部变量?函数内部的
2.可访问范围仅限函数内部  -->  为何只限内部?  执行是创建，结束的时候会销毁局部变量
3.查看局部变量 local()   -->  字典
4.内部函数也看成是一个局部变量。

可变和不可变变量：
值和地址之间的关系
1.不可变变量  in str tuple float boolean
2.可变变量  list dict set

地址的引用: 传递就是一个地址
1.普通的赋值关系
list1 = []
list2 = list1
import sys
sys.getrefcount(list1)

2.函数参数
主要是看参数是可变的还是不可变,会不会改全局变量的参数值。

嵌套
闭包:
    1.必须是嵌套函数
    2.内部函数使用了外部函数变量
    3.返回值是内部函数